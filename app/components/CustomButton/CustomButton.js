import React from 'react';
import './style.scss';

import { Button } from 'react-bootstrap';

const CustomButton = () => (
    <Button
        bsSize="small"
        type="submit"
    >
        Next >
    </Button>
);

export default CustomButton;
